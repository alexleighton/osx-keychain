; dp_probe: retained evidence for the Phase 0.5 signing/scope probe (see PLAN.md
; "Scope"). The Phase 0 round-trip spike was superseded by lib/ + test/.
(executable
 (name dp_probe)
 (foreign_stubs
  (language c)
  (names dp_probe))
 (link_flags
  (-cclib -framework -cclib Security
   -cclib -framework -cclib CoreFoundation)))
